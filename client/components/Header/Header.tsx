import { Navigation } from 'components/Navigation';
import React from 'react';

const Header = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Header;